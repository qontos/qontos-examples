# QONTOS packages — pinned to release tags for reproducibility.
# Update these tags when new versions are released.
# Once published to PyPI, switch to: qontos>=0.2.0, qontos-sim>=0.1.0
git+https://github.com/qontos/qontos.git@v0.2.0
git+https://github.com/qontos/qontos-sim.git@v0.1.0

# Notebook and simulation dependencies
jupyter>=1.0
qiskit>=1.0
qiskit-aer>=0.13
